[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_libs"
version = "1.0.0"
description = ""

license = "MIT"
authors = [
    {name = "Kurochka Vsevolod"}
]

[tool.setuptools]

packages = ["my_libs"]
package-dir = {"my_libs" = "."}